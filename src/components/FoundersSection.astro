<section class="ada-team-section" aria-labelledby="founders-title">
  <div class="ada-manifesto-spacer"></div>

  <div class="ada-manifesto-container">
    <div class="ada-scroll-line-wrapper" aria-hidden="true">
      <div class="ada-scroll-line-fill"></div>
    </div>
    <h3 class="ada-manifesto-text scroll-reveal" id="manifesto-text">
      Bir kapının değeri yalnızca görünüşüyle değil; yıllara meydan okuyan dayanımı ve taşıdığı güvenle ölçülür. Tavizsiz işçilik ve doğru mühendislikle, sadece bir kapı değil güven üretiyoruz.
    </h3>
  </div>

  <div class="ada-subtitle-container">
    <p class="ada-subtitle-text">
      İşi sadece yöneten değil,<br />mutfağında da var olan tecrübeli<br />ekip.
    </p>
  </div>

  <div class="ada-title-container">
    <h4 class="ada-giant-title" id="founders-title">
      <span class="ada-first-letter">Y</span>önetim Kadrosu
    </h4>
  </div>

  <div class="ada-carousel-wrapper">
    <div class="ada-founders-grid">
      <article class="ada-card">
        <div class="ada-card-text">
          <span class="ada-role">Kurucu Ortak</span>
          <h3 class="ada-name">Güven Karaboğa</h3>
        </div>
        <div class="ada-image-container">
          <img src="https://freepngimg.com/save/22654-man/400x400" class="ada-person-img" alt="Güven Karaboğa" loading="lazy" />
          <div class="ada-hover-overlay">
            <p class="ada-bio">Üretim bandından tasarıma kadar her detayın markanın premium standartlarına uymasını sağlar.</p>
            <a
              href="#"
              class="ada-read-more"
              data-name="Güven Karaboğa"
              data-role="Ege Kardoor Partner & Founder"
              data-image="https://freepngimg.com/save/22654-man/400x400"
              data-bio="Güven Karaboğa, Ege Kardoor'un üretim ve tasarım standartlarını belirleyen kurucu güçtür. Konutlardan ticari yapılara kadar geniş bir yelpazede, malzeme mühendisliği ve kusursuz işçiliği bir araya getirerek markanın premium vizyonunu oluşturmuştur."
              data-services="Premium Kapı Tasarımı, Üretim Optimizasyonu, Kalite Kontrol Süreçleri, Malzeme Mühendisliği"
              data-exp="20"
            >
              Read More
            </a>
          </div>
        </div>
      </article>

      <article class="ada-card">
        <div class="ada-card-text">
          <span class="ada-role">Kurucu Ortak</span>
          <h3 class="ada-name">Yaşar Karaboğa</h3>
        </div>
        <div class="ada-image-container">
          <img src="https://freepngimg.com/save/22654-man/400x400" class="ada-person-img" alt="Yaşar Karaboğa" loading="lazy" />
          <div class="ada-hover-overlay">
            <p class="ada-bio">Kardoor'un finansal operasyonlarını ve stratejik saha yönetimini koordine eder.</p>
            <a
              href="#"
              class="ada-read-more"
              data-name="Yaşar Karaboğa"
              data-role="Ege Kardoor Partner & Founder"
              data-image="https://freepngimg.com/save/22654-man/400x400"
              data-bio="Finansal operasyonlar ve stratejik büyüme hedeflerini koordine eden Yaşar Karaboğa, saha yönetimi ve müşteri ilişkileri konusundaki derin tecrübesiyle projelerin kusursuz tamamlanmasını sağlar."
              data-services="Finansal Planlama, Stratejik Saha Yönetimi, Proje Koordinasyonu, Müşteri İlişkileri"
              data-exp="18"
            >
              Read More
            </a>
          </div>
        </div>
      </article>

      <article class="ada-card">
        <div class="ada-card-text">
          <span class="ada-role">Üretim ve Büyüme</span>
          <h3 class="ada-name">Ad Soyad</h3>
        </div>
        <div class="ada-image-container">
          <img src="https://freepngimg.com/save/22654-man/400x400" class="ada-person-img" alt="Ad Soyad" loading="lazy" />
          <div class="ada-hover-overlay">
            <p class="ada-bio">Fabrikadan çıkan her ürünün malzeme ve güvenlik standartlarını bizzat denetler.</p>
            <a
              href="#"
              class="ada-read-more"
              data-name="Ad Soyad"
              data-role="Üretim Direktörü"
              data-image="https://freepngimg.com/save/22654-man/400x400"
              data-bio="Modern üretim teknolojilerini geleneksel el işçiliği disipliniyle harmanlayarak, her kapının yıllara meydan okumasını sağlar."
              data-services="Güvenlik Denetimi, Ar-Ge Çalışmaları, Tedarik Zinciri, Personel Eğitimi"
              data-exp="12"
            >
              Read More
            </a>
          </div>
        </div>
      </article>
    </div>
  </div>
</section>

<div class="ada-modal" id="teamModal" aria-hidden="true">
  <div class="ada-modal-overlay"></div>
  <div class="ada-modal-wrapper" role="dialog" aria-modal="true" aria-labelledby="modalName">
    <div class="ada-modal-left">
      <img src="" alt="" id="modalImage" />
    </div>
    <div class="ada-modal-right">
      <div class="ada-modal-top">
        <div class="ada-modal-location">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path><circle cx="12" cy="10" r="3"></circle></svg>
          İzmir, Türkiye
        </div>
        <button class="ada-modal-close" id="closeModal" type="button">
          Close
          <span class="close-icon">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#000" stroke-width="2.5" aria-hidden="true"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>
          </span>
        </button>
      </div>
      <h2 class="ada-modal-name" id="modalName"></h2>
      <p class="ada-modal-role" id="modalRole"></p>
      <div class="ada-modal-content">
        <div class="ada-modal-bio-section">
          <div class="ada-modal-social" aria-hidden="true"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#000" stroke-width="2"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line></svg></div>
          <p class="ada-modal-bio-text" id="modalBio"></p>
        </div>
        <div class="ada-modal-services-section">
          <span class="services-title">Uzmanlık &amp; Sorumluluklar:</span>
          <ul class="services-list" id="modalServices"></ul>
          <div class="ada-modal-experience"><strong id="modalExp"></strong><span>yıllık tecrübe</span></div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&display=swap");

  .ada-team-section,
  .ada-team-section * {
    box-sizing: border-box;
  }

  .ada-team-section {
    padding: 0 0 120px;
    background-color: #14151d;
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    color: #eae8e8;
    overflow: hidden;
  }

  .ada-manifesto-spacer {
    width: 100%;
    height: 75vh;
  }

  .ada-manifesto-container {
    width: 100%;
    max-width: 1831.59px;
    margin: 0 auto clamp(150px, 18vw, 280px);
    padding: 0 42px;
    display: flex;
    justify-content: flex-start;
    gap: clamp(20px, 3vw, 50px);
  }

  .ada-scroll-line-wrapper {
    width: 3px;
    background-color: rgba(255, 255, 255, 0.15);
    border-radius: 3px;
    position: relative;
    overflow: hidden;
    flex-shrink: 0;
    margin-top: 20px;
    margin-bottom: 10px;
  }

  .ada-scroll-line-fill {
    width: 100%;
    height: 100%;
    background-color: #eae8e8;
    transform-origin: top;
    transform: scaleY(0);
  }

  .ada-manifesto-text {
    font-family: "Instrument Serif", serif;
    font-size: clamp(40px, 7.5vw, 160px);
    font-weight: 400;
    line-height: 0.95;
    letter-spacing: -0.02em;
    color: #eae8e8;
    max-width: 1400px;
    text-align: left;
    margin: 0;
  }

  :global(.reveal-word) {
    display: inline-block;
    white-space: nowrap;
  }

  :global(.reveal-char) {
    display: inline;
    color: #eae8e8;
    opacity: 0.18;
    will-change: opacity;
  }

  :global(.reveal-word.brand-gradient-word .reveal-char) {
    background: linear-gradient(115deg, #8ee7ff 0%, #4fc3f7 18%, #1e9be0 36%, #1477c9 54%, #0b4f9c 72%, #4fc3f7 88%, #8ee7ff 100%);
    background-size: 260% 100%;
    background-position: 0% 50%;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: adaBrandGradientCharMove 6.5s ease-in-out infinite alternate;
  }

  @keyframes adaBrandGradientCharMove {
    0% { background-position: 0% 50%; }
    100% { background-position: 100% 50%; }
  }

  .ada-subtitle-container {
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: 60px;
    margin-bottom: clamp(120px, 20vw, 350px);
  }

  .ada-subtitle-text {
    font-size: clamp(16px, 1.2vw, 24px);
    font-weight: 500;
    line-height: 1.4;
    text-align: center;
    max-width: 360px;
    margin: 0 auto;
    background: linear-gradient(115deg, #aae7ff 0%, #54cfff 25%, #18a8f1 50%, #54cfff 75%, #aae7ff 100%);
    background-size: 260% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: adaGradientTextMove 6.5s ease-in-out infinite alternate;
  }

  @keyframes adaGradientTextMove {
    0% { background-position: 0% 50%; }
    100% { background-position: 100% 50%; }
  }

  .ada-title-container {
    width: 100%;
    display: flex;
    justify-content: center;
    margin-bottom: 40px;
    overflow: visible;
  }

  .ada-giant-title {
    font-family: "Instrument Serif", serif;
    font-size: clamp(70px, 14vw, 320px);
    font-weight: 400;
    line-height: 1.4;
    padding-top: 40px;
    padding-bottom: 20px;
    letter-spacing: -0.01em;
    color: #eae8e8;
    text-align: center;
    white-space: nowrap;
    width: 100%;
    overflow: visible;
    margin: 0;
  }

  :global(.ada-first-letter) {
    display: inline-block;
    margin-right: -0.02em;
  }

  :global(.ada-title-float-word) {
    display: inline-block;
    white-space: nowrap;
  }

  :global(.ada-title-float-char) {
    display: inline-block;
    will-change: transform, opacity;
  }

  .ada-carousel-wrapper {
    width: 100%;
    max-width: 2000px;
    margin: 0 auto;
    overflow: visible;
  }

  .ada-founders-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 42px;
    width: 100%;
    padding: 0 42px;
  }

  .ada-card {
    background-color: #ffffff;
    border-radius: 30px;
    width: 100%;
    height: 940px;
    padding-top: 32px;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    transition:
      background-color 0.8s cubic-bezier(0.165, 0.84, 0.44, 1),
      transform 0.8s cubic-bezier(0.165, 0.84, 0.44, 1);
    cursor: pointer;
    position: relative;
  }

  .ada-card:hover {
    background-color: #000000;
    transform: translateY(-8px);
  }

  .ada-card-text {
    text-align: center;
    z-index: 10;
    margin-bottom: 20px;
    position: relative;
  }

  .ada-role {
    font-size: 16px;
    color: rgba(66, 67, 70, 0.4);
    display: block;
    margin-bottom: 21px;
    transition: color 0.8s cubic-bezier(0.165, 0.84, 0.44, 1);
  }

  .ada-name {
    font-family: "Instrument Serif", serif;
    font-size: clamp(48px, 6vw, 72px);
    font-weight: 400;
    color: #424346;
    line-height: 1;
    margin: 0;
    transition: color 0.8s cubic-bezier(0.165, 0.84, 0.44, 1);
  }

  .ada-card:hover .ada-name {
    color: #ffffff;
  }

  .ada-card:hover .ada-role {
    color: rgba(255, 255, 255, 0.6);
  }

  .ada-image-container {
    position: relative;
    width: 100%;
    flex: 1 1 0;
    min-height: 0;
    display: flex;
    align-items: flex-end;
    justify-content: center;
    overflow: hidden;
  }

  .ada-person-img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: bottom;
    transform-origin: bottom center;
    transition: transform 0.8s cubic-bezier(0.165, 0.84, 0.44, 1);
  }

  .ada-card:hover .ada-person-img {
    transform: scale(1.06);
  }

  .ada-hover-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    padding: 40px 30px 30px;
    background: linear-gradient(to top, rgba(0, 0, 0, 1) 10%, rgba(0, 0, 0, 0.7) 60%, transparent 100%);
    display: flex;
    flex-direction: column;
    align-items: center;
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity 0.8s cubic-bezier(0.165, 0.84, 0.44, 1),
      transform 0.8s cubic-bezier(0.165, 0.84, 0.44, 1);
    z-index: 2;
  }

  .ada-card:hover .ada-hover-overlay {
    opacity: 1;
    transform: translateY(0);
  }

  .ada-bio {
    color: #eae8e8;
    font-size: 16px;
    text-align: center;
    margin: 0 0 20px;
  }

  .ada-read-more {
    position: relative;
    color: #eae8e8;
    text-decoration: none;
    font-size: 26.3px;
    padding-bottom: 6px;
  }

  .ada-read-more::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: 0;
    width: 100%;
    height: 1px;
    background: currentColor;
    transform: scaleX(0);
    transform-origin: center center;
    transition: transform 0.8s cubic-bezier(0.165, 0.84, 0.44, 1);
  }

  .ada-card:hover .ada-read-more::after {
    transform: scaleX(1);
  }

  .ada-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    z-index: 9999;
    display: flex;
    opacity: 0;
    visibility: hidden;
    transition: 0.5s ease;
  }

  .ada-modal.active {
    opacity: 1;
    visibility: visible;
  }

  .ada-modal-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.85);
    backdrop-filter: blur(10px);
  }

  .ada-modal-wrapper {
    position: relative;
    width: 100vw;
    height: 100vh;
    display: flex;
    background: linear-gradient(145deg, rgb(19, 20, 28) 0%, rgb(35, 38, 52) 100%);
    transform: translateY(30px);
    transition: 0.8s cubic-bezier(0.165, 0.84, 0.44, 1);
  }

  .ada-modal.active .ada-modal-wrapper {
    transform: translateY(0);
  }

  .ada-modal-left {
    flex: 0 0 45%;
    height: 100%;
    overflow: hidden;
  }

  .ada-modal-left img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .ada-modal-right {
    flex: 0 0 55%;
    height: 100%;
    padding: 60px;
    display: flex;
    flex-direction: column;
    color: #eae8e8;
    overflow-y: auto;
    border-left: 1px solid rgba(255, 255, 255, 0.1);
  }

  .ada-modal-right::-webkit-scrollbar {
    width: 6px;
  }

  .ada-modal-right::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.2);
    border-radius: 10px;
  }

  .ada-modal-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 20px;
    margin-bottom: 60px;
  }

  .ada-modal-location {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: rgba(234, 232, 232, 0.72);
    font-size: 13px;
  }

  .ada-modal-close {
    background: none;
    border: none;
    color: #eae8e8;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 13px;
  }

  .close-icon {
    width: 34px;
    height: 34px;
    background: #eae8e8;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .close-icon svg {
    stroke: #14151d;
  }

  .ada-modal-name {
    font-family: "Instrument Serif", serif;
    font-size: clamp(52px, 6vw, 80px);
    line-height: 1;
    margin: 0 0 10px;
    font-weight: 400;
  }

  .ada-modal-role {
    color: #888;
    font-size: 16px;
    margin: 0 0 50px;
    font-weight: 500;
  }

  .ada-modal-content {
    display: flex;
    gap: 50px;
    margin-top: auto;
  }

  .ada-modal-bio-section {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .ada-modal-social {
    width: 45px;
    height: 45px;
    background: #222;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .ada-modal-social svg {
    stroke: #eae8e8;
  }

  .ada-modal-bio-text {
    color: #aaa;
    font-size: 15px;
    line-height: 1.6;
    font-weight: 300;
    margin: 0;
  }

  .ada-modal-services-section {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .services-title {
    color: #888;
    font-size: 12px;
    margin-bottom: 15px;
    display: block;
  }

  .services-list {
    list-style: none;
    margin: 0 0 30px;
    padding: 0;
  }

  .services-list li {
    padding: 15px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    font-size: 14px;
    display: flex;
    justify-content: space-between;
    opacity: 0.8;
  }

  .services-list li::after {
    content: "↗";
    opacity: 0.5;
    font-size: 16px;
  }

  .ada-modal-experience {
    text-align: right;
    margin-top: auto;
    display: flex;
    flex-direction: column;
    line-height: 1.2;
  }

  .ada-modal-experience strong {
    font-size: 28px;
    font-weight: 600;
  }

  .ada-modal-experience span {
    color: #888;
    font-size: 13px;
  }

  @media (max-width: 1024px) {
    .ada-subtitle-text {
      font-size: 18px;
      line-height: 1.5;
      max-width: 85%;
    }

    .ada-manifesto-container {
      padding: 0 20px;
    }

    .ada-title-float-word {
      display: inline-block;
      white-space: nowrap;
    }

    .ada-title-float-char {
      display: inline-block;
      will-change: transform, opacity;
    }

    .ada-carousel-wrapper {
      width: 100vw;
      max-width: none;
      position: relative;
      left: 50%;
      right: 50%;
      z-index: 5;
      margin-left: -50vw;
      margin-right: -50vw;
      margin-top: 56px;
      overflow-x: auto;
      overflow-y: visible;
      scroll-snap-type: x mandatory;
      scroll-behavior: smooth;
      padding: 0 0 130px;
      opacity: 1;
      visibility: visible;
      scrollbar-width: none;
      -ms-overflow-style: none;
    }

    .ada-carousel-wrapper::-webkit-scrollbar {
      display: none;
    }

    .ada-founders-grid {
      --ada-mobile-card-width: 76vw;
      display: flex;
      flex-wrap: nowrap;
      align-items: stretch;
      gap: 5vw;
      width: max-content;
      min-width: max-content;
      padding-left: calc((100vw - var(--ada-mobile-card-width)) / 2);
      padding-right: calc((100vw - var(--ada-mobile-card-width)) / 2);
      opacity: 1;
      visibility: visible;
    }

    .ada-card {
      flex: 0 0 var(--ada-mobile-card-width);
      width: var(--ada-mobile-card-width);
      height: 90vh;
      min-height: 720px;
      max-height: 980px;
      padding-top: 28px;
      scroll-snap-align: center;
      scroll-snap-stop: always;
      opacity: 1;
      visibility: visible;
      transform: none;
    }

    .ada-card[data-loop-clone="true"] {
      opacity: 1;
      visibility: visible;
    }

    .ada-card-text {
      flex: 0 0 auto;
      margin-bottom: 12px;
    }

    .ada-image-container {
      flex: 1 1 auto;
      width: 100%;
      height: auto;
      min-height: 0;
      display: flex;
      align-items: flex-end;
      justify-content: center;
      overflow: hidden;
      padding: 0;
      opacity: 1;
      visibility: visible;
    }

    .ada-person-img {
      display: block;
      width: 108%;
      height: 100%;
      max-width: none;
      max-height: none;
      object-fit: contain;
      object-position: center bottom;
      align-self: flex-end;
      margin: 0;
      opacity: 1;
      visibility: visible;
      transform: translateY(14px);
    }

    .ada-card:hover .ada-person-img {
      transform: translateY(14px) scale(1.04);
    }

    .ada-modal-wrapper {
      flex-direction: column;
      overflow-y: auto;
    }

    .ada-modal-left {
      height: 40vh;
      flex: none;
    }

    .ada-modal-right {
      flex: none;
      border-left: none;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      padding: 40px 25px;
      height: auto;
    }

    .ada-modal-content {
      flex-direction: column;
      gap: 30px;
      margin-top: 0;
    }
  }

  @media (max-width: 768px) {
    .ada-carousel-wrapper {
      margin-top: 52px;
      padding: 0 0 112px;
    }

    .ada-founders-grid {
      --ada-mobile-card-width: 76vw;
      gap: 5vw;
    }

    .ada-card {
      height: 88vh;
      min-height: 700px;
      max-height: 940px;
    }

    .ada-person-img {
      width: 110%;
      transform: translateY(16px);
    }

    .ada-card:hover .ada-person-img {
      transform: translateY(16px) scale(1.04);
    }
  }

  @media (max-width: 560px) {
    .ada-manifesto-spacer {
      height: 42vh;
    }

    .ada-manifesto-text {
      font-size: clamp(38px, 12vw, 68px);
    }

    .ada-title-container {
      margin-bottom: 12px;
    }

    .ada-giant-title {
      font-size: clamp(54px, 18vw, 92px);
      white-space: normal;
    }

    .ada-card {
      min-height: 620px;
      border-radius: 24px;
    }

    .ada-name {
      font-size: 46px;
    }

    .ada-hover-overlay {
      opacity: 1;
      transform: translateY(0);
    }

    .ada-modal-top {
      margin-bottom: 34px;
    }
  }
</style>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  const scrollLineFill = document.querySelector(".ada-scroll-line-fill");
  if (scrollLineFill) {
    gsap.to(scrollLineFill, {
      scaleY: 1,
      ease: "none",
      scrollTrigger: {
        trigger: "#manifesto-text",
        start: "top 85%",
        end: "bottom 50%",
        scrub: 2
      }
    });
  }

  const revealElement = document.getElementById("manifesto-text");
  if (revealElement) {
    const text = revealElement.textContent?.trim().replace(/\s+/g, " ") ?? "";
    const words = text.split(" ");
    revealElement.innerHTML = "";

    words.forEach((word, i) => {
      const wordSpan = document.createElement("span");
      wordSpan.className = "reveal-word";
      if (word.includes("Ege") || word.includes("Kardoor")) wordSpan.classList.add("brand-gradient-word");

      Array.from(word).forEach((char) => {
        const charSpan = document.createElement("span");
        charSpan.className = "reveal-char";
        charSpan.textContent = char;
        wordSpan.appendChild(charSpan);
      });
      revealElement.appendChild(wordSpan);
      if (i < words.length - 1) revealElement.appendChild(document.createTextNode(" "));
    });

    const chars = revealElement.querySelectorAll(".reveal-char");

    gsap.fromTo(
      chars,
      {
        opacity: 0.18
      },
      {
        opacity: 1,
        stagger: 0.02,
        ease: "none",
        scrollTrigger: {
          trigger: "#manifesto-text",
          start: "top 85%",
          end: "bottom 50%",
          scrub: 2
        }
      }
    );
  }

  const titleElement = document.querySelector<HTMLElement>(".ada-giant-title");

  if (titleElement) {
    const titleText = titleElement.textContent?.trim().replace(/\s+/g, " ") ?? "";
    titleElement.innerHTML = "";

    titleText.split(" ").forEach((word, wordIndex, wordArray) => {
      const wordSpan = document.createElement("span");
      wordSpan.className = "ada-title-float-word";

      Array.from(word).forEach((char, charIndex) => {
        const charSpan = document.createElement("span");
        charSpan.className = "ada-title-float-char";

        if (wordIndex === 0 && charIndex === 0) {
          charSpan.classList.add("ada-first-letter");
        }

        charSpan.textContent = char;
        wordSpan.appendChild(charSpan);
      });

      titleElement.appendChild(wordSpan);

      if (wordIndex < wordArray.length - 1) {
        titleElement.appendChild(document.createTextNode(" "));
      }
    });

    const titleChars = titleElement.querySelectorAll(".ada-title-float-char");

    gsap.fromTo(
      titleChars,
      {
        opacity: 0,
        yPercent: 120,
        scaleY: 2.3,
        scaleX: 0.7,
        transformOrigin: "50% 0%"
      },
      {
        opacity: 1,
        yPercent: 0,
        scaleY: 1,
        scaleX: 1,
        duration: 1,
        stagger: 0.01,
        ease: "back.inOut(2)",
        scrollTrigger: {
          trigger: titleElement,
          start: "center bottom+=50%",
          end: "bottom bottom-=40%",
          scrub: true
        }
      }
    );
  }

  if (window.matchMedia("(min-width: 1025px)").matches) {
    gsap.from(".ada-card", {
      y: 80,
      opacity: 0,
      stagger: 0.25,
      duration: 1.8,
      ease: "power4.out",
      scrollTrigger: {
        trigger: ".ada-title-container",
        start: "top 70%",
        once: true
      }
    });
  }

  const modal = document.getElementById("teamModal");
  const closeModalBtn = document.getElementById("closeModal");
  let clickPrevented = false;

  const openTeamModal = (btn: HTMLElement) => {
    if (!modal || clickPrevented) return;

    const modalName = document.getElementById("modalName");
    const modalRole = document.getElementById("modalRole");
    const modalBio = document.getElementById("modalBio");
    const modalImage = document.getElementById("modalImage") as HTMLImageElement | null;
    const modalExp = document.getElementById("modalExp");
    const sList = document.getElementById("modalServices");

    if (!modalName || !modalRole || !modalBio || !modalImage || !modalExp || !sList) return;

    modalName.textContent = btn.dataset.name ?? "";
    modalRole.textContent = btn.dataset.role ?? "";
    modalBio.textContent = btn.dataset.bio ?? "";
    modalImage.src = btn.dataset.image ?? "";
    modalImage.alt = btn.dataset.name ?? "";
    modalExp.textContent = btn.dataset.exp ?? "";

    sList.innerHTML = "";

    if (btn.dataset.services) {
      btn.dataset.services.split(",").forEach((s) => {
        const li = document.createElement("li");
        li.textContent = s.trim();
        sList.appendChild(li);
      });
    }

    modal.classList.add("active");
    modal.setAttribute("aria-hidden", "false");
    document.body.style.overflow = "hidden";
  };

  document.addEventListener("click", (e) => {
    const target = e.target as Element | null;
    const btn = target?.closest<HTMLElement>(".ada-read-more");
    if (!btn) return;

    e.preventDefault();
    openTeamModal(btn);
  });

  const closeModal = () => {
    if (!modal) return;

    modal.classList.remove("active");
    modal.setAttribute("aria-hidden", "true");
    document.body.style.overflow = "";
  };

  if (closeModalBtn) closeModalBtn.addEventListener("click", closeModal);

  if (modal) {
    modal.addEventListener("click", (e) => {
      const target = e.target as Element;
      if (target === modal || target.classList.contains("ada-modal-overlay") || target.classList.contains("ada-modal-wrapper")) {
        closeModal();
      }
    });
  }

  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape") closeModal();
  });

  const carouselWrapper = document.querySelector<HTMLElement>(".ada-carousel-wrapper");
  const slider = document.querySelector<HTMLElement>(".ada-founders-grid");

  if (carouselWrapper && slider) {
    const carousel = carouselWrapper;
    const track = slider;
    const mobileQuery = window.matchMedia("(max-width: 1024px)");
    let loopActive = false;
    let jumping = false;
    let resizeTimer: ReturnType<typeof setTimeout> | null = null;

    const getOriginalCards = () => Array.from(track.querySelectorAll<HTMLElement>(".ada-card:not([data-loop-clone='true'])"));

    const cleanCardInlineStyles = (card: HTMLElement) => {
      card.style.opacity = "";
      card.style.visibility = "";
      card.style.transform = "";

      card.querySelectorAll<HTMLElement>("*").forEach((el) => {
        el.style.opacity = "";
        el.style.visibility = "";
        el.style.transform = "";
      });
    };

    function removeLoop() {
      carousel.removeEventListener("scroll", handleLoopScroll);

      track.querySelectorAll(".ada-card[data-loop-clone='true']").forEach((clone) => clone.remove());

      track.querySelectorAll<HTMLElement>(".ada-card[data-loop-original='true']").forEach((card) => {
        card.removeAttribute("data-loop-original");
        cleanCardInlineStyles(card);
      });

      loopActive = false;
      jumping = false;
    }

    const getCenteredScrollLeft = (card: HTMLElement) => {
      return card.offsetLeft - (carousel.clientWidth - card.offsetWidth) / 2;
    };

    const getLoopMetrics = () => {
      const originals = Array.from(track.querySelectorAll<HTMLElement>(".ada-card[data-loop-original='true']"));
      const firstOriginal = originals[0];
      const firstAfterClone = track.querySelector<HTMLElement>(".ada-card[data-loop-clone-position='after']");

      if (!firstOriginal || !firstAfterClone) return null;

      const start = getCenteredScrollLeft(firstOriginal);
      const end = getCenteredScrollLeft(firstAfterClone);
      const length = end - start;

      if (length <= 0) return null;

      return { start, end, length };
    };

    const jumpTo = (left: number) => {
      jumping = true;

      const oldBehavior = carousel.style.scrollBehavior;
      carousel.style.scrollBehavior = "auto";
      carousel.scrollLeft = left;

      requestAnimationFrame(() => {
        carousel.style.scrollBehavior = oldBehavior;
        requestAnimationFrame(() => {
          jumping = false;
        });
      });
    };

    function handleLoopScroll() {
      if (!mobileQuery.matches || !loopActive || jumping) return;

      const metrics = getLoopMetrics();
      if (!metrics) return;

      if (carousel.scrollLeft < metrics.start) {
        jumpTo(carousel.scrollLeft + metrics.length);
        return;
      }

      if (carousel.scrollLeft >= metrics.end) {
        jumpTo(carousel.scrollLeft - metrics.length);
      }
    }

    const initLoop = () => {
      removeLoop();

      if (!mobileQuery.matches) return;

      const originals = getOriginalCards();
      if (originals.length < 2) return;

      const beforeFragment = document.createDocumentFragment();
      const afterFragment = document.createDocumentFragment();

      originals.forEach((card) => {
        card.setAttribute("data-loop-original", "true");
        cleanCardInlineStyles(card);
      });

      originals.forEach((card) => {
        const beforeClone = card.cloneNode(true) as HTMLElement;
        beforeClone.setAttribute("data-loop-clone", "true");
        beforeClone.setAttribute("data-loop-clone-position", "before");
        beforeClone.removeAttribute("data-loop-original");
        cleanCardInlineStyles(beforeClone);
        beforeFragment.appendChild(beforeClone);
      });

      originals.forEach((card) => {
        const afterClone = card.cloneNode(true) as HTMLElement;
        afterClone.setAttribute("data-loop-clone", "true");
        afterClone.setAttribute("data-loop-clone-position", "after");
        afterClone.removeAttribute("data-loop-original");
        cleanCardInlineStyles(afterClone);
        afterFragment.appendChild(afterClone);
      });

      track.insertBefore(beforeFragment, originals[0]);
      track.appendChild(afterFragment);

      loopActive = true;

      requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          const firstOriginal = track.querySelector<HTMLElement>(".ada-card[data-loop-original='true']");

          if (firstOriginal) {
            jumpTo(getCenteredScrollLeft(firstOriginal));
          }

          carousel.addEventListener("scroll", handleLoopScroll, { passive: true });

          ScrollTrigger.refresh();
        });
      });
    };

    initLoop();

    window.addEventListener("resize", () => {
      if (resizeTimer) clearTimeout(resizeTimer);
      resizeTimer = setTimeout(initLoop, 180);
    });

    if (mobileQuery.addEventListener) {
      mobileQuery.addEventListener("change", initLoop);
    } else {
      mobileQuery.addListener(initLoop);
    }

    window.addEventListener("load", () => {
      if (mobileQuery.matches) {
        initLoop();
      }
    });
  }

  window.addEventListener("load", () => {
    ScrollTrigger.refresh();
  });
</script>
