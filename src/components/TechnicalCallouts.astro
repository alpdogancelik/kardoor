---
interface Props {
  labels: string[];
}

const { labels } = Astro.props;
const safeLabels = labels.slice(0, 6);
---

<div class="technical-callouts" aria-hidden="true">
  {safeLabels.map((label) => (
    <span data-callout>{label}</span>
  ))}
</div>